Rules:
+ AND
| OR
^ XOR
! NOT
( ) PARENTHESIS
=> IMPLIES

Node:
Facts nodes which correspond to fact
Rules nodes which link facts node together

Node:
boolean active
operator overload + | ^ !

Fact nodes:
name

Rules nodes:
boolean rules

Keep track of all Fact Node to activate them
when needed

Backtrack from destination to origin to see if
the destination node is active

Parsing File:
See example.txt for input example

Scanner:
get each character from file as well as column and line

Lexer:
compare character with known characters
call corresponding function

Token:
string -> token string
type -> token type
int col -> column
int line -> line
